using System;

class Program {
  public static void Main (string[] args) {
   int num = 8;
   if (num == 10)
      Console.WriteLine("입력된 값이 10 입니다.");
    else if(num==9)
      Console.WriteLine("입력된 값이 9입니다.");
    else
      Console.WriteLine("입력된 값이 9와 10이 아닙니다.");
  }
}